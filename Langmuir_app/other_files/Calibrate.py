from guizero import App, Text
from tkinter import *

    

def Cal_simple():
    #get hx arg
    #generate ui
    #tare hx
    #ask for known input mass
    #return cal value
    pass

def Cal_complex():
    #generate ui
    #tare hx
    #ask for known masses from low to high
        #save each value and prompt for next
        #use raw data values
    #generate force function
    #get r^2 value
    #return cal value and r^2 value
    pass

def Calibrate():
    # Create the main window
    window = Tk()
    window.title("Langmuir APP")

    # Create the main container
    #frame = Frame(window)
    #frame.configure(bg='white')

    # Lay out the main container (expand to fit window)
    #frame.pack(fill=BOTH, expand=1)

    # Create figure for plotting
    #fig = figure.Figure(figsize=(2, 2))

    #dfont = tkFont.Font(size=-20)

    #canvas_plot = canvas.get_tk_widget()
    
    window.mainloop()
    

Calibrate()
